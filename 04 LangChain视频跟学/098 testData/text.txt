目标：实现F2业务0低跑节点原因的初步自动分析归纳。
实现指标：
一级：（出现则不做后续判断）
心跳超时！！！
二级：（出现后不做三级判断）
只跑高峰期,不做处理
18-23点带宽跑满率大于75%,不做处理
nat类型为对称型,不做处理
三级：
硬件资源型归因：
带宽磁盘比 1:2
cpu长时间跑满（大于90%时间段超50%）
内存长时间跑满（大于80%时间段超50%）
根据带宽判断是否符合最低配置。（基于表格参数）


系统负载19-23点均值高于100
业务型归因：
启动一天时间业务缓存低于5% or 启动三天业务缓存不足60%
实例数启动不符合预期：磁盘带宽比低于1：3情况下，42868实例数高于预期数  or  42868实例数低于预期数60%  or 42868实例数低于2
业务重传率均值>8
网络型归因：
线路状态异常，网络不通
存在同省、异网标签
晚高峰限速节点
针对归因处理建议：
硬件资源型归因建议根据归因进行硬件调整，
业务型归因根据归因进行特定分析或业务重新编排，
网络型归因暂无优化方案
多个标签归因原因，一般来说：网络型>硬件资源型>业务型，但具体原因具体分析



nat类型
STUN 协议则是为了解决 P2P 通信中的 NAT 穿透问题而提出的。这个协议中定义了四种 NAT 类型：

nat1:完全锥型 NAT 是最简单的一种 NAT 类型，它的特点是：内网里同一个 IP 和 Port，都会被映射为同一个公网 IP 和 Port，而且任何外部主机都可以通过向映射后的公网 IP 和 Port 发送数据，这些数据会直接转发到内网 IP 和 Port 上。

nat2:限制锥形NAT：NAT2 则比 NAT1 多了个条件，不再是任何外部主机发送到公网 IP 和 Port 都会被转发到内网，而是内网主机主动联系过这个 IP，这个 IP 才能向内网主机发送数据。（这个 IP 的任何端口都可以发送数据到内网）

nat3：端口限制锥形NAT：NAT3 比 NAT2 更加严格，不仅要求内网主机联系过这个 IP，同时也要求端口号也联系过。也就是说，只有内网主机主动联系过这个 IP 和 Port，这个 IP 和 Port 才能向内网主机发送数据。（同 IP 下其他的 Port 发送的 UDP 数据会被丢弃）

nat4：对称型NAT：NAT4 是最严格的一种 NAT 类型，NAT1 的要求（内网里同一个 IP 和 Port，都会被映射为同一个公网 IP 和 Port）不再遵守了。如果联系不同的 IP 和 Port，那么映射的公网 IP 和 Port 也会不同。（IP 和 Port 任何一个有变化，都会映射到不同的公网 IP 和 Port），同时他还有 NAT3 的限制，只有内网主机主动联系过这个 IP 和 Port，这个 IP 和 Port 才能向内网主机发送数据。

安全性系数上看：对称型 > 端口受限锥型 > 受限锥型 > 全锥型；相应的，对称型nat对P2P支持最差。

穿透性：

1、full cone 全椎
2、Restricted Cone ip受限
3、port Restricted Cone 端口受限
4、Symmetric 对称。
​
穿透关系：上述4种类型，如果定义他们的值为序列号，N为序列号相加。
则N<=6可穿透。2 和 4 序列相加 = 6 则可穿透。 
3(端口受限)和4（对称）= 7 不可穿透。
宽表中nat类型 对应关系 （upnp为打洞后nat类型）：

       0.未知

卓越公网；网卡绑定公网IP

全锥；

IP限制；

端口限制；

对称；

DMZ公网；可以认定为全锥。

设备类型：
X86：XRVD，XYBM

玩客云：OCPZ，OCPG，OCTZ

oe设备：EA，EC，ES

0低跑
概念：一般看上行带宽占用率，0跑即为业务没有跑动，低跑则为业务占用率低。
常见命令
查看网络：/tmp/multidialstatus.json

查看agent log：/xyapp/system/plugin-mdata/agent_log/go_agent.log

查看网络配置是否支持拨号：cat /etc/xyapp/k8s_network.json

查看调度任务： cat /xyapp/system/plugin-mdata/dockerTasksRecord.json | python -m json.tool | egrep -w 'status|name|seq'

查看磁盘负载：iostat -x 30 2 | grep -v zram | grep -v loop | grep -v nb

磁盘负载保护黑名单：黑名单记录到本地文件：x86路径 /etc/xyapp/disk_overload.json, oe路径/app/system/plugin-mdata/xyapp/disk_overload.json

b站查看重传缓存

containers=$(docker ps --no-trunc --format "{{.ID}}-{{.Names}}" | grep 42868)
for i in $containers
    do
    cid=$(echo $i | awk -F'-' '{print $1}')
    name=$(echo $i | awk -F'-' '{print $2}')
    echo "---------- $cid ------------ $name ---"
    docker exec -it $name cat /storage/centaurs.json | grep -E 'retransmission_rate|send_speed|nat_type'
done
​

计划：
在V1业务中混跑（跑量抢不过爱奇艺和hpg，跑的好的和B站可以持平）
目前存在问题：
阿里小节点曲线存在毛刺，峰值时间掉量情况。
客户测试期间给了新的pcdn程序进行替换，需要客户重新打包镜像升级。客户预计下周一发版新镜像（用户已发版，开始测试新镜像）。
目前测试设备中，好的测试样例单实例跑20M，差的只跑2M，初步判断和运营商有关，异网移动缓存很快，起量也快，异网电联起量慢一些，容易低跑。
agent适配优化（已出临时版本，功能测试正常）：
客户侧新增调度逻辑，需要在容器/storage/meta/mineconfig.ini新增字段bandwidth_Mbps = 30，该字段为建设带宽，客户会选择5M以上从高开始调度，提前传入有助于加快调度。客户侧跑几天后会修正该数值，测试传过高效果不大。
之前agent版本中，根据资源标签进行了异网功能配置文件自动适配，但阿里25491业务为电联、移动分开计费。如资源标签出现异网移动、异网联通双标签情况，则会直接按照移动计费，统计时会出现计费差异。需要针对这种多异网资源标签情况进行过滤，如多异网资源标签中同时包含异网电联、异网移动，则文件配置中忽略异网电联标签。移动标签优先级高于电联。
预期交付要求：
CPU大于等于32核，磁盘单实例50-100G
优先交移动，本网移动或者异网移动都可以，移动预期缓存两天完成，单实例20M-30M，电联预期缓存3天，单实例3-7M。
异网标签不同时包含移动和电联。(agent,支持后可以按移动去交付跑）